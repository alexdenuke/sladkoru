import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false,
  // experimental: {
  //   scrollRestoration: true, // Автоматическое восстановление прокрутки
  // },
};

export default nextConfig;
